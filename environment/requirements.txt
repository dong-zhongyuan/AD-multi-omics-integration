# Python dependencies for "A Neural ODE framework for cross-tissue regulatory mapping"
# Pinned for reproducibility. Python 3.10.11.
#
# Install:  pip install -r environment/requirements.txt
#
# Tools imported by name from code/tools/ (NOT pip — clone them; see docs/DATA_AND_CODE.md §3):
#   Geneformer  https://huggingface.co/ctheodoris/Geneformer   -> code/tools/geneformer-main/
#   GenKI       https://github.com/yjgeno/GenKI                -> code/tools/GenKI-master/
#   MultiXrank  https://github.com/anthbapt/multixrank         -> code/tools/multixrank/
#
# Pip-installable third-party packages below (sources: their GitHub repos):
#   POT         https://github.com/PythonOT/POT
#   torchdiffeq https://github.com/rtqchen/torchdiffeq
#   networkx    https://github.com/networkx/networkx
#   lifelines   https://github.com/CamDavidsonPilon/lifelines

# ── Core scientific stack ──
numpy==1.26.4
scipy==1.11.4
pandas==2.1.4
scikit-learn==1.3.2
statsmodels==0.14.1

# ── Deep learning (Neural ODE core) ──
torch==2.1.2
# Note: torchdiffeq is vendored in code/tools/hepaworld/models/dynamics.py (custom ODE integrator + Sinkhorn OT).
# No external OT library (ott-jax/geomloss/POT) is required.

# ── Single-cell & omics ──
anndata==0.10.3
scanpy==1.9.6
leidenalg==0.10.1

# ── Network / ranking ──
networkx==3.2.1

# ── Survival analysis (step5) ──
lifelines==0.27.7

# ── Gene ID mapping ──
mygene==3.2.2

# ── Geneformer (transformer virtual knockout) ──
transformers==4.36.2
datasets==2.16.1
accelerate==0.25.0

# ── Plotting ──
matplotlib==3.8.2
seaborn==0.13.0

# ── Config ──
pyyaml==6.0.1
