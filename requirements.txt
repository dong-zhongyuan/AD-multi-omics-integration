# AD Multi-Omics Integration Pipeline — Python Dependencies
# Python 3.10+
#
# Install: pip install -r requirements.txt

# ── Core scientific computing ──
numpy>=1.26,<3.0
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
sympy>=1.12

# ── Deep learning ──
torch>=2.0
torch-geometric>=2.4
lightning>=2.0            # GenKI/PDGrapher training
accelerate>=1.0           # HuggingFace acceleration

# ── Single-cell / bioinformatics ──
scanpy>=1.10
anndata>=0.10
h5py>=3.10
loompy>=3.0               # SCENIC data format

# ── GRN inference (SCENIC VK) ──
pyscenic>=0.12
arboreto>=0.1             # GRNBoost2 backend
ctxcore>=0.2              # SCENIC dependency

# ── Network analysis ──
networkx>=3.1
igraph>=1.0

# ── Survival analysis ──
lifelines>=0.27

# ── Data / API ──
requests>=2.31
pyyaml>=6.0
tqdm>=4.66
pyarrow>=15.0             # datasets/huggingface dependency
datasets>=2.14            # data loading
dask>=2024.0              # arboreto parallel backend

# ── Visualization ──
matplotlib>=3.8
seaborn>=0.13

# ── Gene ID mapping ──
mygene>=3.2

# ── High-performance computing ──
numba>=0.59               # scanpy JIT acceleration
llvmlite>=0.42            # numba backend
joblib>=1.3               # sklearn parallel
ray>=2.0                  # arboreto distributed backend (optional)

# ── Motif/GO analysis (SCENIC pipeline) ──
genomepy>=0.16            # genome reference
goatools>=1.5             # GO enrichment

# ── Parsing / I/O ──
lxml>=5.0                 # XML parsing (KEGG, etc.)
jinja2>=3.1               # templating

# ── ODE solver (Neural ODE engine) ──
# torchdiffeq — included in tools/ directory, not pip installable from PyPI
# Install separately: pip install torchdiffeq

# ── OT solver (Sinkhorn) ──
# POT (Python Optimal Transport) — included in tools/ directory
# Install separately: pip install POT

# ── Pathway enrichment ──
gseapy>=1.3

# ── Plotting (figures) ──
plotly>=6.0
python-pptx>=0.6
Pillow>=10.0

# ── R packages (install via Rscript) ──
# ggplot2, circlize, ggridges, ggsankeyfier, patchwork, rayshader
# Install: Rscript -e 'install.packages(c("ggplot2","circlize","ggridges","patchwork","rayshader")); remotes::install_github("SAngiamo/ggsankeyfier")'
